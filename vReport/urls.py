from django.conf.urls import patterns, include, url
from vReport.views import *

urlpatterns = patterns('',
    # url(r'^$', 'vitamin.views.home', name='home'),
    # url(r'^vitamin/', include('vitamin.foo.urls')),
)
